apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap
  namespace: namespace55
data:
  pierwszyklucz-lubiacy-values: "{{ .Values.ala }}"
  drugiklucz-lubiacy-releases: "{{ .Release.Name }}"
  trzeci-lubi-charty: "{{ .Chart.Name }}"
